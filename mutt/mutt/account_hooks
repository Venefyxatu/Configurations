# Make sure no imap_user lingers when switching to any imap account
account-hook . 'unset imap_user' 

# For every server, set the imap_user.  
# Don't set the password - I'll enter it when prompted

account-hook "imaps://venefyxatu@imap.gmail.com" \
  'set imap_user=venefyxatu@gmail.com'

account-hook "imaps://heerener@imap.gmail.com" \
  'set imap_user=heerener@gmail.com'

account-hook "imaps://aschermcentrum@imap.gmail.com" \
  'set imap_user=aschermcentrum@gmail.com'

account-hook "imaps://evolve.wings@imap.gmail.com" \
  'set imap_user=evolve.wings@gmail.com'
